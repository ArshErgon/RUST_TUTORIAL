fn main() {
    // let number = 10;

    // normal control flow

    // if number > 19 {
    //     println!("Big Number");
    // } else if number == 0 {
    //     println!("zero number");
    // } else {
    //     println!("{number}")
    // }

    // single line if and else conditions

    // let find_the_number = if number != 0 { 90 } else { 20 };
    // println!("{find_the_number}");

    // infinite loop syntax

    // let mut num = 0;
    // loop {
    //     println!("Hello! {num}");
    //     num += 1;
    // }

    // let mut counter = 0;

    // using loop in a variable

    // let loop_variable = loop {
    //     counter += 1;

    //     if counter == 10 {
    //         break counter;
    //     }
    // };

    // println!("{loop_variable}");

    // using lables in the loops to stop a particular loop when condition match the requirement

    // let mut count = 0;

    // 'stop_loop_one: loop {
    //     println!("loop one is working");
    //     count += 1;
    //     if count >= 5 {
    //         break 'stop_loop_one;
    //     }
    //     'stop_loop_two: loop {
    //         println!("loop two is working");
    //         count += 1;
    //         if count >= 3 {
    //             break 'stop_loop_two;
    //         }
    //     }
    // }

    // using a while loop

    // let array = [1, 2, 3, 4, 5];
    // let mut idx = 0;

    // while idx < 5 {
    //     println!("{}", array[idx]);
    //     idx += 1;
    // }

    // using a for loop basic syntax

    // let array = [1, 2, 3, 4, 5];
    // let mut idx = 0;
    // for ele in array {
    //     println!("element at idx:{idx} = {ele}");
    //     idx += 1;
    // }

    // range function [ for i in 1..4  ] start - end

    // for i in (1..10).rev() {
    //     println!("{i}");
    // }

}
